<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace = "aboutme">
  <!-- 항목 등록-->
  <insert id="create" parameterType="AboutmeVO">
  INSERT into aboutme(am_no, 
                    am_title, am_content )
  VALUES((SELECT NVL(MAX(am_no), 0)+1 as am_no FROM aboutme), 
                    #{am_title}, #{am_content})
  </insert>

  <!-- 자소서 목록 -->
  <select id="list" resultType="AboutmeVO">
    SELECT am_no, am_title, am_content
    FROM aboutme
    ORDER BY am_no DESC
  </select>

  <!-- 읽어옴  -->
  <select id="read" resultType="AboutmeVO">
    SELECT am_no, am_title, am_content
    FROM aboutme
    WHERE am_no = #{am_no}
  </select>  
  
  <!-- 수정 -->
  <update id="update" parameterType="AboutmeVO">
    UPDATE aboutme
    SET am_title=#{am_title}, am_content=#{am_content} 
    WHERE am_no=#{am_no}
  </update> 
  
  <!-- 삭제 -->
  <delete id="delete" parameterType="int">
    DELETE FROM aboutme
    WHERE am_no=#{am_no}
  </delete>
    
</mapper>